# No Specs Required

This change is a bug fix that does not introduce new capabilities or modify existing spec-level behavior. The fix restores expected macOS standard behavior (text editing shortcuts) without changing requirements.

No specification files are needed for this change.
